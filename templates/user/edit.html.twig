{% extends 'base.html.twig' %}

{% block title %}{{'Mettre à jour les informations'|trans}}{% endblock %}

{% block body %}
    <a href="{{ path('app_user_index') }}" class="text-muted">{{ 'Retour'|trans }}</a>

    <div class="d-flex flex-row justify-content-between align-items-end">
        <h1>{{'Mettre à jour les informations'|trans}}</h1>
        {{ include('user/_delete_form.html.twig') }}
    </div>

    {{ include('user/_form.html.twig', {'button_label': 'Update'|trans}) }}


{% endblock %}
