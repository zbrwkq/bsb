{% extends 'base.html.twig' %}

{% block title %}BSB | Modification de produit{% endblock %}

{% block body %}
    <h1>Modification de {{ produit.nom }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_produit_show', {'id': produit.id}) }}" class="btn btn-secondary">Retour au produit</a>
{% endblock %}
