{% extends 'base.html.twig' %}

{% block title %}BSB | {{'produit.modifier'|trans}}{% endblock %}

{% block body %}
    <a href="{{ path('app_produit_show', {'id': produit.id}) }}" class="text-muted">{{'Retour'|trans}}</a>
    
    <h1>Modification de {{ produit.nom }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
